<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:fitsSystemWindows="true">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorSurface"
        app:titleTextColor="?attr/colorOnSurface" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="24dp">

            <TextView
                style="@style/AboutBody"
                android:text="@string/about_intro" />

            <TextView style="@style/AboutHead" android:text="@string/about_goggle_head" />
            <TextView style="@style/AboutBody" android:text="@string/about_goggle_body" />

            <TextView style="@style/AboutHead" android:text="@string/about_connect_head" />
            <TextView style="@style/AboutBody" android:text="@string/about_connect_body" />

            <TextView style="@style/AboutHead" android:text="@string/about_video_head" />
            <TextView style="@style/AboutBody" android:text="@string/about_video_body" />

            <TextView style="@style/AboutHead" android:text="@string/about_recovery_head" />
            <TextView style="@style/AboutBody" android:text="@string/about_recovery_body" />
        </LinearLayout>
    </ScrollView>

</LinearLayout>
